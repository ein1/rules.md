# 웹 접근성 구현 지침

> **최종 개정 일시: 2022-12-28 00:00 (KST)**
>
> **이 문서를 수정하면 위 최종 개정 일시를 같은 커밋에서 갱신한다.**
>
> **근거 표준: 한국형 웹 콘텐츠 접근성 지침 2.2 — 국가표준 `KS X OT0003`, 2022-12-28 개정 고시 (국립전파연구원, https://www.rra.go.kr)**

이 문서는 한국형 웹 콘텐츠 접근성 지침의 핵심 내용을 개발 에이전트가 바로 구현과 점검에 활용할 수 있도록 재구성한 작업 지침서다. 원문에 섞여 있던 페이지 안내, 해설성 문장, 중복 설명은 걷어내고, 구현 규칙과 예외, 설계 시 유의점을 중심으로 정리했다.

## 적용 범위

- 이 지침은 PC, 모바일, 터치스크린 기반 기기 등에서 실행되는 모든 웹 콘텐츠에 적용한다.
- 서비스 제공자가 제공하는 모든 콘텐츠를 대상으로 하며, 제3자가 제공하는 서비스도 포함한다.
- 전체 구조는 4개 원칙, 14개 지침, 33개 검사항목으로 이루어진다.
- 원칙은 인식의 용이성, 운용의 용이성, 이해의 용이성, 견고성으로 구성된다.

## 구현 원칙

### 1. 인식의 용이성

#### 대체 텍스트

- 텍스트가 아닌 콘텐츠에는 의미나 용도를 인식할 수 있는 대체 텍스트를 제공한다.
- 이미지 링크, 이미지 버튼처럼 기능이 분명한 요소는 핵심 기능을 짧고 명확하게 설명한다.
- 의미가 있는 배경 이미지는 보조기술이 그 의미를 전달할 수 있도록 처리한다.
- 데이터 차트처럼 복잡한 시각 요소는 사용자가 내용을 충분히 이해할 수 있을 정도의 설명을 제공한다.
- 장식용 이미지는 불필요한 낭독을 피하기 위해 공백 대체 텍스트로 처리한다.
- 동일한 정보를 이미 텍스트로 제공했다면 보조기술이 중복 전달하지 않도록 구성한다.

#### 멀티미디어 대체수단

- 영상·음성 등 멀티미디어 콘텐츠에는 자막, 대본 또는 수어를 최소 하나 이상 제공한다.
- 대체수단은 원래 콘텐츠의 음성, 대사, 문맥과 동등해야 한다.
- 영상만 있는 경우에는 화면해설 성격의 텍스트, 오디오 또는 대본을 제공한다.
- 음성만 있는 경우에도 자막, 대본 또는 수어를 제공한다.

#### 적응성

- 표는 제목 셀과 데이터 셀을 구분해 구조를 이해할 수 있도록 작성한다.
- 콘텐츠는 보조기술이 읽는 순서 기준으로 논리적인 선형구조를 유지해야 한다.
- CSS로 화면 배치를 바꾸더라도 문서 구조 자체의 의미 순서는 무너지지 않게 설계한다.
- 표현과 구조와 기능을 분리해, 스타일이 제거되더라도 문맥을 파악할 수 있어야 한다.

#### 지시사항과 시각 정보

- 지시사항은 색, 위치, 모양, 크기, 방향, 소리 같은 단일 감각 정보에만 의존하지 않는다.
- 예를 들어 '오른쪽 버튼', '빨간 버튼'처럼만 안내하지 말고, 실제 버튼명이나 역할을 함께 제시한다.
- 정답 여부나 상태 변화를 소리로만 알려주지 말고, 시각적 또는 텍스트 수단을 함께 제공한다.
- 차트, 그래프, 상태 표시는 색만으로 구분하지 말고 패턴, 라벨, 기호 등을 함께 사용한다.

#### 명도 대비와 자동 재생

- 일반적인 텍스트 콘텐츠는 배경과의 명도 대비를 4.5:1 이상 확보한다.
- 큰 글자 또는 굵은 글자, 확대 가능한 콘텐츠는 조건에 따라 3:1까지 완화할 수 있다.
- 자동 재생 소리는 원칙적으로 피하고, 3초 이상 재생된다면 멈춤, 일시정지, 음량 조절 수단을 제공한다.
- 이웃한 콘텐츠는 테두리, 간격, 배경색 차이, 구분선 등으로 시각적으로 분리한다.

### 2. 운용의 용이성

#### 키보드 접근성

- 웹에서 제공하는 모든 기능은 키보드만으로 사용할 수 있어야 한다.
- 위치 지정 도구의 궤적 자체가 본질인 기능은 예외가 될 수 있지만, 그 외 UI는 키보드로 접근 가능해야 한다.
- 초점 이동 순서는 사용자의 예상과 일치하도록 논리적으로 구성한다.
- 초점이 위치한 요소는 테두리, 밑줄, 색상 변화 등으로 명확하게 보여야 하며, 표시가 사라지지 않아야 한다.
- 사용자 입력 요소와 콘트롤은 충분한 크기로 제공해 선택과 조작이 가능해야 한다.

#### 문자 단축키

- 단일 문자 단축키를 제공한다면 오동작 방지 장치를 반드시 둔다.
- 사용자가 단축키를 끌 수 있어야 하거나, 기능키 조합으로 재설정할 수 있어야 하거나, 해당 요소에 초점이 있을 때만 작동해야 한다.

#### 시간제한과 자동 변경 콘텐츠

- 시간제한이 있는 콘텐츠는 가능하면 사용하지 않는다.
- 보안 등으로 시간제한이 필요하다면, 시간제한을 해제하거나 연장할 수 있는 수단을 제공한다.
- 시간제한 종료 전에는 충분한 사전 안내를 제공해야 하며, 최소 20초 이상의 여유를 두는 것이 요구된다.
- 자동 스크롤, 자동 갱신, 자동 전환 콘텐츠는 정지, 이전, 다음 같은 제어 수단을 제공해야 한다.

#### 깜빡임과 번쩍임

- 초당 3회에서 50회 사이로 깜빡이거나 번쩍이는 콘텐츠는 제공하지 않는다.
- 불가피하다면 정지할 수 있어야 하고, 번쩍임 시간은 3초 미만으로 제한하는 것이 바람직하다.
- 특히 큰 화면에서 번쩍이는 면적이 화면의 상당 부분을 차지하지 않도록 주의한다.

#### 내비게이션

- 반복되는 메뉴와 링크 영역은 건너뛸 수 있어야 하며, 핵심 영역으로 가는 건너뛰기 링크를 페이지 앞부분에 제공한다.
- 여러 건너뛰기 링크가 있다면 핵심 영역으로 이동하는 링크를 가장 먼저 제공한다.
- 페이지, 팝업, 프레임, 콘텐츠 블록에는 내용을 드러내는 제목을 붙인다.
- 제목은 간단명료해야 하며, 동일 사이트 안에서 서로 다른 페이지 제목은 중복되지 않아야 한다.
- 링크 텍스트는 목적과 용도를 이해할 수 있어야 하며, '여기', '더보기', 'GO' 같은 표현만 단독으로 사용하지 않는다.
- 전자출판 형식의 웹 콘텐츠는 페이지 번호 같은 참조 위치 정보를 일관되게 제공하고, 각 위치로 이동할 수 있어야 한다.

#### 입력 방식

- 멀티터치, 스와이프, 드래그처럼 다중 포인터 또는 경로기반 입력으로 제공되는 기능은 단일 포인터로도 수행 가능해야 한다.
- 단일 포인터 입력으로 실행되는 기능은 취소할 수 있어야 한다.
- 화면에 보이는 레이블이 있는 UI 구성요소는 접근성 네임에도 같은 텍스트를 포함해야 한다.
- 흔들기 같은 동작기반 기능은 일반 UI로도 조작 가능해야 하며, 사용자가 해당 기능을 끌 수 있어야 한다.

### 3. 이해의 용이성

#### 언어와 예측 가능성

- 웹 페이지의 기본 언어를 명시해 보조기술이 올바른 방식으로 처리할 수 있게 한다.
- 사용자가 명시적으로 요청하지 않았는데 갑자기 새 창이 열리거나 페이지가 이동하는 식의 맥락 변화는 피한다.
- 도움말, 문의, 지원 정보는 사용자가 쉽게 찾을 수 있는 위치에 제공한다.

#### 입력 도움

- 입력 오류가 발생하면 어디에서 어떤 문제가 생겼는지, 어떻게 수정해야 하는지 알려준다.
- 모든 입력 콘트롤에는 역할과 목적을 알 수 있는 레이블을 제공한다.
- 인증 과정은 기억력, 독해력, 숫자 계산 능력에 과도하게 의존하지 않도록 설계한다.
- 여러 단계에서 같은 정보를 반복 입력해야 한다면 자동 채움 또는 선택 입력 수단을 제공한다.
- 다만 비밀번호처럼 보안상 재입력이 필요한 정보는 예외가 될 수 있다.

### 4. 견고성

#### 마크업 문법

- 마크업 요소는 시작 태그와 종료 태그가 올바르게 대응해야 한다.
- 중첩 관계가 어긋나지 않도록 문서 구조를 유지한다.
- 동일 요소 안에서 속성을 중복 선언하지 않는다.
- 동일 문서 안에서 id 값은 중복되지 않아야 한다.

#### 웹 애플리케이션 접근성

- 웹 애플리케이션과 플러그인은 보조기술 사용자의 접근을 방해하지 않아야 한다.
- 운영체제나 플랫폼이 제공하는 접근성 API를 활용해 이름, 역할, 상태, 값을 전달할 수 있어야 한다.
- 국내 보조기술과 호환되지 않는 기능은 가능하면 피하고, 반드시 사용해야 한다면 대체수단을 제공한다.

## 구현 체크리스트

### 마크업

- 모든 페이지에 고유한 title이 있는가.
- heading 구조가 논리적인가.
- 폼 요소에 label이 연결되어 있는가.
- id 값 중복이 없는가.
- 테이블에 제목 셀과 데이터 셀이 적절히 구분되어 있는가.

### 상호작용

- 마우스 없이 키보드만으로 모든 기능을 사용할 수 있는가.
- 초점 이동 순서가 시각적 흐름 및 업무 흐름과 맞는가.
- 초점 표시가 눈에 띄고 사라지지 않는가.
- 드래그, 스와이프, 멀티터치 기능에 대체 입력 수단이 있는가.
- 단일 문자 단축키 오동작 방지 장치가 있는가.

### 콘텐츠

- 모든 이미지와 비텍스트 요소에 적절한 대체 텍스트가 있는가.
- 장식용 이미지는 스크린리더가 무시하도록 처리했는가.
- 영상과 음성 콘텐츠에 자막, 대본, 수어 중 하나 이상이 있는가.
- 색만으로 의미를 전달하는 요소가 없는가.
- 텍스트 명도 대비가 기준을 충족하는가.

### 동작과 흐름

- 자동 재생 소리가 3초 이상 지속되지 않도록 제어되는가.
- 자동 갱신·자동 스크롤 콘텐츠에 정지 기능이 있는가.
- 시간제한이 있을 경우 연장 또는 해제 수단이 있는가.
- 깜빡임·번쩍임 효과가 발작 유발 기준에 걸리지 않는가.
- 반복 영역 건너뛰기 링크가 제공되는가.

## 신규 검사항목 반영 포인트

2022년 개정본은 기존 24개 검사항목에서 33개 검사항목으로 확대되었고, 특히 모바일·터치·인지 접근성과 관련된 항목이 강화되었다. 구현 시에는 문자 단축키, 단일 포인터 입력, 포인터 입력 취소, 레이블과 네임 일치, 동작기반 작동, 고정된 참조 위치 정보, 찾기 쉬운 도움 정보, 접근 가능한 인증, 반복 입력 정보 항목을 별도 점검 대상으로 두는 것이 좋다.

## 문서 사용 방식

이 문서는 표준 해설서가 아니라 구현 지침서로 사용해야 한다. 설계 단계에서는 컴포넌트 요구사항 정의에, 개발 단계에서는 마크업과 상호작용 구현 기준에, QA 단계에서는 접근성 점검 체크리스트의 기준 문서로 활용하는 것이 적절하다.
